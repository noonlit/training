$quiz->where({{condition}})->load();
$quiz->join({{table}}, [column1 => column2]);

# explicitly define associated entities and keep a field that gets populated if you perform the join and doesn't if you don't?
# is there a generic way to execute queries?
# can the entities have a query builder? 
# load() -> lazy load every entity associated?

class Quiz
{
  QueryBuilder $queryBuilder;
  Questions[] $questions;
  int $id;
  
  public function load()
  {
    return $this->queryBuilder->select()->from('quiz')->where('id = $this->$id'); ... something
  }

}
